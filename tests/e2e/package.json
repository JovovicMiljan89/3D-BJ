{
  "name": "3d-bj-e2e",
  "private": true,
  "type": "module",
  "description": "Optional Playwright smoke tests, kept out of the root package.json so the site itself stays dependency-free.",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
