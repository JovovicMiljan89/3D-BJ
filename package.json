{
  "name": "3d-bj-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for 3D-BJ, a 3D printing service by Bojan Jovović — content editable via Pages CMS, hosted on Cloudflare Pages.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "format": "npx --yes prettier --write ."
  }
}
